export interface MovieDetailsProps {
  imdbID: string;
  title: string;
  year: string;
  poster: string;
  imdbRating: number;
  plot: string;
  released: string;
  actors: string;
  director: string;
  genre: string;
  runtime: number;
  userRating?: number;
}
